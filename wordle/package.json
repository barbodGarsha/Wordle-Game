{
  "name": "wordle",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && git add dist -f && git commit -am \"Build and Deploy\" && cd .. && git subtree push --prefix wordle/dist origin deploy"
  },
  "dependencies": {
    "sass": "^1.69.7",
    "vue": "^3.3.11"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^4.5.2",
    "vite": "^5.0.10"
  }
}
